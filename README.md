# minigpt-jax-GeminiStack
Built a 20M parameter GPT style language model from scratch using JAX covering data preprocessing, transformer architecture (Flax/NNX), optimized training (Optax), checkpointing (Orbax), and end-to-end text generation via a chat interface.
